version: "3.8"

services:
  trellis2-server:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: trellis2-unity-studio
    runtime: nvidia
    ports:
      - "8000:8000"
      - "7860:7860"
    volumes:
      - ../outputs:/app/outputs
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
